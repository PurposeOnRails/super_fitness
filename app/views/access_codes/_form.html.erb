<%= form_with(model: access_code, local: true) do |form| %>
  <% if access_code.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(access_code.errors.count, "error") %> prohibited this access_code from being saved:</h2>

      <ul>
      <% access_code.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <div class="form-group"><%= form.label :description %>
      <%= form.text_field :description, :class=>'form-control' %></div>
  </div>

  <div class="field">
    <div class="form-group"><%= form.label :purpose_id %>
      <%= form.collection_select(:purpose_id, Purpose.all, :id, :name, {}, {:class=>'form-control'}) %></div>
  </div>

    <%= form.submit :class=>'btn btn-primary' %>

<% end %>
