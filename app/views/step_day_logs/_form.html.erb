<%= form_with(model: step_day_log, local: true) do |form| %>
  <% if step_day_log.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(step_day_log.errors.count, "error") %> prohibited this step_day_log from being saved:</h2>

      <ul>
      <% step_day_log.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :step_count %>
    <%= form.number_field :step_count %>
  </div>

  <div class="field">
    <%= form.label :date %>
    <%= form.date_select :date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
